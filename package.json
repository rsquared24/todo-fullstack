{
  "name": "todos-fullstack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "bootstrap": "npm run install:api; npm run install:core",
    "build": "npm run build:api; npm run build:core",
    "build:api": "cd ./api; npm run build",
    "install:api": "cd ./api; npm install",
    "test:api": "cd ./api; npm run test",
    "build:core": "cd ./core; npm run build",
    "install:core": "cd ./core; npm install",
    "test:core": "cd ./core; npm run test",
    "install:web": "cd ./web; npm install"
  },
  "author": "",
  "license": "ISC",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "wsrun": "^3.6.4"
  }
}
